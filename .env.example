# When set, the app talks to a self-hosted Directus instance instead of the
# in-browser localStorage demo. Leave empty to keep the demo mode.
#
# Example (on-premise / Azure UAE):
# VITE_DIRECTUS_URL=https://api.alrahaib.ae
VITE_DIRECTUS_URL=
